name: Vercel Production Deployment
run-name: Vercel Production Deployment
env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
on:
  push:
    branches:
      - main
jobs:
  Deploy-Production:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Pull Vercel Environment Information
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}

      - name: Build Project Artifacts
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy Project Artifacts to Vercel
        id: deploy
        run: |
          vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }} --yes

          # paksa isi URL utama (alias kamu)
          echo "url=https://github-vercel-five.vercel.app/" >> $GITHUB_OUTPUT

      - name: Debug URL
        run: echo "${{ steps.deploy.outputs.url }}"

      - name: Wait for deployment
        run: sleep 15

      - name: Check deployment result
        run: curl -I ${{ steps.deploy.outputs.url }}
  

